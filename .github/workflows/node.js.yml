name: Node.js CI - Personal-Site-Remastered.js

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.16.0'
      - run: npm install
      - run: node Personal-Site-Remastered.js
      # - run: npm run build --if-present
      # - run: npm test